# haskell-api

```
apt-get update
apt-get install docker docker-compose

cd .devcontainer
docker-compose build
docker-compose up

curl -sSL https://get.haskellstack.org/ | sh
stack install
stack run
```
